# This requirements.txt is shared by two projects in this repository.
# It includes dependencies for:
# 1) Gen_AI_APIs (OpenAI and Gemini examples)
# 2) agents/book_recommender (LangChain + MCP agent for book recommendations)

# Dependencies for both projects
openai==1.97.1
python-dotenv==1.1.1
google-generativeai==0.8.5

# Data processing
pandas==2.3.1

# MCP framework for building custom tools
fastmcp==2.10.6

# LangChain and related libraries (used in the book recommender agent)
langchain-openai==0.3.28
langgraph==0.6.0
langchain_mcp_adapters==0.1.9